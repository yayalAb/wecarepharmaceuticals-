# Odoo 18 Docker Deployment - Environment Variables
# Copy this file to .env and adjust values for your environment

# PostgreSQL
POSTGRES_USER=odoo
POSTGRES_PASSWORD=odoo18@2021
POSTGRES_DB=postgres

# Odoo (must match PostgreSQL credentials)
HOST=db
USER=odoo
PASSWORD=odoo18@2021

# Odoo database manager (master password for create/restore/delete DB)
# Required to secure the database manager at /web/database/manager
ADMIN_PASSWD=change_me_use_strong_password

# Optional: Odoo configuration
# ODOO_EXTRA_ADDONS=/mnt/extra-addons
# ODOO_CONFIG=/etc/odoo/odoo.conf

# SSL (automatic via certbot container — no manual setup-ssl.sh needed)
SSL_DOMAIN=erp.kadglobaltrading.com
CERTBOT_EMAIL=admin@kadglobaltrading.com
# CERTBOT_RETRY_SECONDS=300
# CERTBOT_RENEW_INTERVAL=43200
