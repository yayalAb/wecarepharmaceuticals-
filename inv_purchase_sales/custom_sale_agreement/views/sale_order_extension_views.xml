<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_sale_order_inherit_pay_req" model="ir.ui.view">
        <field name="name">sale.order.form.pay.req</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <!-- Smart Button -->
            <!-- <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_payment_requests"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-money"
                        invisible="payment_request_count == 0">
                    <field name="payment_request_count" widget="statinfo" string="Requests"/>
                </button>
            </xpath> -->

            <!-- Create Button: Only visible in 'sale' (Confirmed/Approved) state -->
            <xpath expr="//header" position="inside">
                <button name="action_create_payment_request" 
                        string="Request Payment" 
                        type="object" 
                        class="btn-primary"
                        invisible="state != 'sale'"/>
            </xpath>
        </field>
    </record>
</odoo>