<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="action_report_sale_agreement" model="ir.actions.report">
            <field name="name">Sales Contract</field>
            <field name="model">sale.agreement</field>
            <field name="report_type">qweb-pdf</field>
            <!-- The name of your report template -->
            <field name="report_name">custom_sale_agreement.report_sale_agreement_document</field>
            <field name="report_file">custom_sale_agreement.report_sale_agreement_document</field>
            <field name="print_report_name">'Sales Contract - %s' % (object.name)</field>
            <field name="binding_model_id" ref="agreement.model_sale_agreement"/>
            <field name="binding_type">report</field>
        </record>
    </data>
</odoo>