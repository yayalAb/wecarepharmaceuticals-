<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_supplies_rfp_tree" model="ir.ui.view">
        <field name="name">supplies.rfp.tree</field>
        <field name="model">supplies.rfp</field>
        <field name="arch" type="xml">
            <list string="Request for Purchases">
                <field name="rfp_number" string="Reference" />
                <field name="create_uid" string="Requested By" />
                <field name="requested_date" />
                <field
                    name="state"
                    widget="badge"
                    decoration-warning="state in ('draft',)"
                    decoration-danger="state in ('rejected',)"
                    decoration-info="state in ('approved',)"
                    decoration-primary="state in ('recommendation',)"
                    decoration-success="state in ('accepted',)"
                />
            </list>
        </field>
    </record>

    <record id="view_supplies_rfp_form" model="ir.ui.view">
        <field name="name">supplies.rfp.form</field>
        <field name="model">supplies.rfp</field>
        <field name="arch" type="xml">
            <form string="Purchase Requests">
                <header>
                    <field
                        name="state"
                        widget="statusbar"
                        statusbar_visible="draft,submitted,approved,evaluation,accepted"
                        invisible="state == 'rejected'"
                    />
                    <button
                        name="action_submit"
                        string="Submit"
                        class="oe_highlight"
                        type="object"
                        invisible="state != 'draft'"
                    />
                    <button
                        name="action_approve"
                        string="Approve"
                        class="oe_highlight"
                        type="object"
                        invisible="state != 'submitted'"
                    />
                    <button
                        name="action_reject"
                        string="Reject"
                        class="btn-warning"
                        invisible="state != 'submitted'"
                    />
                    <button
                        name="action_close"
                        string="Close"
                        type="object"
                        class="btn-primary"
                        invisible="state != 'approved'"
                    />
                    <button
                        name="action_accept"
                        string="Accept"
                        type="object"
                        invisible="state != 'evaluation'"
                    />
                    <button
                        name="action_return"
                        string="Return"
                        type="object"
                        invisible="state in ['draft', 'ordered']"
                    />
                    <button
                        name="action_open_po_creation_form"
                        string="Create Quotation"
                        type="object"
                        invisibile="state != 'approved'"
                    />
                    <button
                        name="action_open_po_creation_form"
                        string="Create Order"
                        type="object"
                        invisibile="state != 'approved' or not (purchase_origin == 'local' and purchase_type == 'direct')"
                    />
                    <button
                        name="action_open_po_creation_form"
                        string="Direct Purchase"
                        type="object"
                        invisible="state != 'approved' or not (purchase_origin == 'local' and purchase_type != 'direct')"
                    />
                    <button
                        name="action_create_bid_purchase_orders"
                        string="Create Orders"
                        type="object"
                        invisible="state != 'accepted'"
                    />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_quotations"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-shopping-cart"
                            invisible="rfq_count == 0">
                            <field name="rfq_count" widget="statinfo"
                                string="Quotations" />
                        </button>
                        <button name="action_view_purchase_orders"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-shopping-cart"
                            invisible="state != 'ordered'"/>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="rfp_number" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="product_category_id" />
                            <field name='purchase_origin' widget="radio"
                                readonly="state not in ['draft', 'submitted', 'approved']" />
                            <field name='purchase_type' readonly="state not in ['draft', 'submitted', 'approved']"
                            invisible="state in ['draft', 'submitted']" />
                            <field name="create_uid" string="Requested By" />
                        </group>
                        <group>
                            <field name="required_date" readonly="state != 'draft'" />
                            <field name="department_id" />
                            <field name="company_id" />
                            <field name="store_request_id"
                                readonly="state not in ['draft', 'submitted']" />
                        </group>
                    </group>

                    <notebook>
                        <page string="Product Lines">
                            <field
                                name="product_line_ids"
                                context="{'default_rfp_id': id}"
                                readonly="state not in ['draft', 'submitted']">
                                <list editable="bottom">
                                    <field name="product_id" string="Product" />
                                    <field name="description" string="Description" />
                                    <field name="product_image" widget="image"
                                        options='{"size": [120, 120]}' />
                                    <field name="product_qty" string="Quantity" />
                                    <field name="remaining_qty" string="Remaining Quantity" />
                                    <field name="ordered_qty" string="Ordered Quantity" />
                                    <field name="product_uom" string="UoM" />
                                </list>
                            </field>
                        </page>

                        <page string="Bid Comparison" name='bid_comparison'
                            invisible="state in ['draft','submitted']">
                            <div class="o_action_manager">
                                <button
                                    name="action_recommend_best_prices"
                                    string="Recommend Best Prices"
                                    type="object"
                                    class="btn-primary"
                                    invisible="state != 'evaluation'"
                                    help="Automatically select the RFQ line with the lowest price for each product."
                                />
                            </div>
                            <field name="comparison_table" readonly="1" />

                            <p class="text-muted">
                                * Green cells indicate recommended selections.
                            </p>
                        </page>

                        <page string="Committee Members" name="committee_members"
                            invisible="state == 'draft'">
                            <field name="committee_member_ids">
                                <list editable="bottom">
                                    <field name="member_id" />
                                    <field name="role" />
                                    <field name="approval_status" />

                                    <button name="action_committee_approve"
                                        type="object"
                                        icon="fa-check"
                                        title="Approve"
                                        class="btn-success"
                                        invisible="approval_status != 'pending' or parent.state != 'submitted'" />

                                    <button name="action_committee_reject"
                                        type="object"
                                        icon="fa-times"
                                        title="Reject"
                                        class="btn-danger"
                                        invisible="approval_status != 'pending' or parent.state != 'submitted'" />
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
</odoo>