<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        This action tells Odoo to open the main configuration screen,
        but it cleverly passes a context key.
    -->
    <record id="action_loan_config" model="ir.actions.act_window">
        <field name="name">Loan Policy Configuration</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module' : 'hr_payroll'}</field>
    </record>

    <!--
        This menu item will appear under Payroll > Configuration.
        When clicked, it calls the action above.
    -->
    <menuitem
        id="menu_loan_config"
        name="Loan Policies"
        parent="hr.menu_human_resources_configuration"
        action="action_loan_config"
        sequence="110"/>
</odoo>